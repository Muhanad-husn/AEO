# ai-enterprise-template

A reusable template for a one-person AI software enterprise inside Claude Code:
tool-locked role subagents (triage, spec, test, implement, review) build and check,
while the founder holds architecture and merge authority. Two deterministic hook gates
make the boundary real — agents never merge, and no one commits a red suite. Reuse this
template to start future products.
